# DEVLOG — 2026-06-01

## 범위

- Import 스크립트 **공용 env 로드**·**`current/` 경로 해석** 정리
- Fund Activity Detail: 소수 계정 번호(`1865.1` 등) 파싱 수정
- **서버(DB 근접) 일괄 적재** 운영 가이드 (Ubuntu Go 설치 포함)
- 관련 UI/파싱 문서 소폭 정합

> Import UI(Wails) 구현은 계속 후속. 본일은 스크립트·파서·운영 문서 중심.

---

## 변경 요약

### 1) [`scripts/lib/load_run_all_imports_env.sh`](../scripts/lib/load_run_all_imports_env.sh)

- `run_all_imports.env` CRLF-safe 로드 (`tr -d '\r'`)
- `kcpc_resolve_import_xlsx`: env에 **파일명만** 적으면 `{repo}/current/파일명` 으로 해석
- `kcpc_check_import_db_env`: `DATABASE_URL` 또는 `PG*` 검사
- 모든 `import_*.sh`·`run_all_imports.sh` 에서 source 후 `kcpc_resolve_import_env_paths` 호출

### 2) [`scripts/run_all_imports.env_sample`](../scripts/run_all_imports.env_sample)

- `current/` + 파일명만 예시
- 서버 실행 가이드 링크 한 줄 추가

### 3) Go — [`fund_activity_detail_import.go`](../fund_activity_detail_import.go)

- `isAccountNumber`: `1865.1`, `1865.5` 등 **점(.) 포함 계정 번호** 인식
- `parseNumberText` 가 `strconv.Atoi` 대신 위 검사 사용 → `1865.*` 구간 헤더·행 누락 해소

### 4) 테스트

| 파일 | 내용 |
|------|------|
| [`fund_activity_detail_import_test.go`](../fund_activity_detail_import_test.go) | `1865.1` 등 dotted account 테스트 |
| [`fund_activity_detail_xlsx_scan_test.go`](../fund_activity_detail_xlsx_scan_test.go) | (로컬 xlsx 있을 때) 스캔 검증 |

### 5) 문서

| 문서 | 내용 |
|------|------|
| [`서버_DB근접_일괄적재_실행가이드.md`](서버_DB근접_일괄적재_실행가이드.md) | 로컬 대신 DB 서버에서 `run_all_imports` 실행, **§3 Ubuntu Go 1.22+ 설치**, rsync·env·트러블슈팅 |
| [`FundActivityDetail_TransactionType_파싱_개발방안.md`](FundActivityDetail_TransactionType_파싱_개발방안.md) | dotted account 번호 언급 보강 |
| [`Import_UI_로컬데스크톱_개발방안.md`](Import_UI_로컬데스크톱_개발방안.md) | env/`current/` 규칙·서버 가이드 참조 정리 |

---

## 운영 메모

1. **로컬 env**: `scripts/run_all_imports.env` 에 엑셀은 **파일명만**, 실제 파일은 `current/` 에 둔다.
2. **느린 원격 DB**: 적재는 서버에서 `DATABASE_URL=127.0.0.1` 로 실행 — [`서버_DB근접_일괄적재_실행가이드.md`](서버_DB근접_일괄적재_실행가이드.md).
3. **Ubuntu 최초**: 가이드 §3 → `go version` (1.22+) 확인 후 §5 clone·`go mod download`.
4. **Summary·Special G/L 기간**: export 종료일이 다르면 `period mismatch` — 데이터 재 export.

---

## 이전 일지

- [`DEVLOG_2026-05-19.md`](DEVLOG_2026-05-19.md) — `income_expense_kind` 파서·마이그레이션·Import UI 스펙
- [`DEVLOG_2026-05-08.md`](DEVLOG_2026-05-08.md) — `run_all_imports` dry-run·DB URL

---

## 후속 (미구현)

- [ ] Import UI (Wails) — 스펙 문서 기준
- [ ] `main.go` ministry `fiscalYear` 와 env `FISCAL_YEAR` 정합
