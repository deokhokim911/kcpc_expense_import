# DEVLOG — 2026-06-15

## 범위

- 사역원·부서별 **지출 집계 스냅샷** — 개발 방안 문서·migration·Go 적재 구현 (1차)
- 기존 `org_balance` / `org_budget` / **소비 UI 무변경** 원칙 확정 및 배포 가이드 정리

> UI가 summary 테이블을 조회하도록 바꾸는 것은 **2차(선택)**. 본일은 적재 파이프라인·DDL·문서까지.

---

## 변경 요약

### 1) 문서 — [`사역원_부서별_지출_집계_적재_개발방안.md`](사역원_부서별_지출_집계_적재_개발방안.md)

| 절 | 내용 |
|----|------|
| §1.1 | 기존 테이블·UI **무변경** 호환 원칙 |
| §5 | `org_balance_import_run` + `org_expense_summary_row` 스키마 |
| §6 | 1-pass 집계·트랜잭션 적재 흐름 |
| §14 | **마이그레이션·서버 배포** (psql, git pull, run_all_imports, UI 재배포 불필요) |

### 2) DB migration — [`20260606120000_add_org_expense_summary/migration.sql`](20260606120000_add_org_expense_summary/migration.sql)

- `org_expense_agg_level` ENUM (`ministry` / `department`)
- `org_balance_import_run` — 적재 메타 (연도, 파일명, 기간, 건수)
- `org_expense_summary_row` — 사역원·부서별 `total_expense` 등
- **CREATE만** — `org_balance`·`org_budget` ALTER 없음

### 3) Go — [`org_expense_summary.go`](../org_expense_summary.go)

- `expenseSummaryCollector`: ministry 적재 루프에서 **1-pass** `(segment3, segment1)` 집계
- `total_expense = debitamt + creditamt` (기존 UI SUM과 동일 개념)
- `runMinistryOrgBalanceImport`: 단일 트랜잭션  
  run INSERT → delete `org_balance` → insert `org_balance` → insert summary → commit
- `org_budget` JOIN으로 `budget_amount`·`remaining_budget` 스냅샷
- migration 미적용 시 live 적재 **명확한 에러**

### 4) [`main.go`](../main.go)

- `handleMinistryBalance`: collector 인자 추가, `insertBudgetBalance`로 **org_balance INSERT 동일**
- live ministry: `runMinistryOrgBalanceImport` 호출 (delete+insert 트랜잭션 통합)
- dry-run: `org_expense_summary planned_insert department=N ministry=M` 출력

### 5) 테스트 — [`org_expense_summary_test.go`](../org_expense_summary_test.go)

- 집계·기간 파싱·라벨 추출 단위 테스트

### 6) [`서버_DB근접_일괄적재_실행가이드.md`](서버_DB근접_일괄적재_실행가이드.md)

- 마이그레이션 목록에 `20260606120000_add_org_expense_summary` 추가

### 7) Fund Activity Detail — `classifyAccountCode` Expense 구간 확장

- [`fund_activity_detail_import.go`](../fund_activity_detail_import.go): Expense 하한 **5180 → 5120** (`5120–9010`)
- **원인**: `5120 - Pension` 등이 분류 밖 → Income sticky 유지 → `DF-Staff Pension` 등이 `income_expense_kind=Income` 으로 잘못 적재
- 테스트: `TestClassifyAccountCode` (`5120`, `5179`), `TestParseGeneralLedgerDetailLines_5120PensionExpense`
- 문서: [`FundActivityDetail_TransactionType_파싱_개발방안.md`](FundActivityDetail_TransactionType_파싱_개발방안.md) 규칙 갱신
- **반영**: Special G/L **재적재** 필요 (`import_fund_activity_fiscal_year.sh` 또는 `run_all_imports.sh`)

---

## 운영·배포 (요약)

```bash
# 1) DB (서버)
psql "$DATABASE_URL" -v ON_ERROR_STOP=1 \
  -f 2026/20260606120000_add_org_expense_summary/migration.sql

# 2) 코드
cd /opt/kcpc/onfinance && git pull && go mod download

# 3) 적재 (summary 자동 생성)
./scripts/run_all_imports.sh --dry-run
./scripts/run_all_imports.sh
```

- **소비 UI/API**: 재배포·재시작 **불필요**
- 상세: 개발 방안 **§14**, 서버 가이드

### 적재 후 확인

```sql
SELECT id, fiscalyear, source_filename, detail_row_count, imported_at
FROM org_balance_import_run ORDER BY imported_at DESC LIMIT 1;

SELECT aggregation_level, COUNT(*), SUM(total_expense)
FROM org_expense_summary_row
WHERE run_id = (SELECT id FROM org_balance_import_run ORDER BY imported_at DESC LIMIT 1)
GROUP BY aggregation_level;
```

---

## 분석·수정 (5120 Pension)

| 항목 | 내용 |
|------|------|
| 증상 | `5120 - Pension` 아래 `DF-Staff Pension` → DB `income_expense_kind=Income` |
| 원인 | Expense 구간이 5180부터라 `5120` 미분류 → 4510 Income sticky 유지 |
| 수정 | `n >= 5120 && n <= 9010` → Expense |

---

## 이전 일지

- [`DEVLOG_2026-06-01.md`](DEVLOG_2026-06-01.md) — env/`current/`·서버 가이드·`1865.1` 파서
- [`DEVLOG_2026-05-19.md`](DEVLOG_2026-05-19.md) — `income_expense_kind`

---

## 후속 (미구현)

- [ ] UI/API: `org_expense_summary_row` 조회 전환 (2차)
- [ ] `main.go` `fiscalYear` ↔ env `FISCAL_YEAR` 정합
- [ ] §11 검증 SQL 자동화 (적재 직후 diff 0건)
